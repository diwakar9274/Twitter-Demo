<section class="section">
  <div class="container">
    <div class="columns">
      <%= render 'trends' %>
      <%= render 'feed' %>
      <%= render 'who_to_follow' %>
    </div>
  </div>
</section>

<p id="notice"><%= notice %></p>
<%= link_to 'New Tweeet', new_tweeet_path %>
